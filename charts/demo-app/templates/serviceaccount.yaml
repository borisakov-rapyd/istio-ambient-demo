# Dedicated ServiceAccount = the workload's mesh identity (SPIFFE).
# This is what AuthorizationPolicies match on — identity, not IP.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "demo-app.name" . }}
  labels:
    {{- include "demo-app.labels" . | nindent 4 }}
